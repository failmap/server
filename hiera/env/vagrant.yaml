# configuration specific to Vagrant dev/test environment
classes:
  # configure and run docker daemon
  - docker

# use docker friendly firewall purge on hosted environment
base::end::hosted::docker: true
