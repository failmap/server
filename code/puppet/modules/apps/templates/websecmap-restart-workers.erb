#!/bin/bash

# exit on every error
set -e

# restart all services
systemctl stop docker-websecmap-worker-\*.service
systemctl start docker-websecmap-worker-\*.service

echo
echo "Workers restarted!"
echo
